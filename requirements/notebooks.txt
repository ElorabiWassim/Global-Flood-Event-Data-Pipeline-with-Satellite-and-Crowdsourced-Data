# Optional dependencies for running the analytical notebooks under
# `notebooks/`. The runtime pipeline (Airflow DAG + FastAPI) does NOT need
# these — they are only used in Jupyter for ad-hoc analysis.
#
# Install locally with:
#     pip install -r requirements/notebooks.txt

-r base.txt

# --- Notebook runtime ---
jupyterlab==4.2.5
ipykernel==6.29.5

# --- Plotting ---
matplotlib==3.9.2
seaborn==0.13.2

# --- Time-series / stats ---
statsmodels==0.14.2
scikit-learn==1.5.1
